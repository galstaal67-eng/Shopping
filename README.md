# 🛒 אפליקציית רשימת קניות

אפליקציית ווב (קובץ HTML יחיד, ללא שרת) שנבנתה לפי העיקרון של הקובץ
"רשימת קניות.xlsx" — קטלוג מוצרים קבוע לפי קטגוריות, סימון מה שצריך,
ורשימת קנייה נוחה למי שמבצע את הקנייה.

## שימוש

פותחים את `index.html` בדפדפן (בנייד או במחשב). הנתונים נשמרים
בדפדפן (localStorage) — אין צורך בהתקנה או חשבון.

### מסך "מה צריך" 📝
- קטלוג המוצרים מהאקסל המקורי (כולל ההערות), מועשר במוצרים נפוצים נוספים
  ובקטגוריות חדשות (לחם ומאפים, קפואים ושתייה). קטגוריית הירקות מפוצלת
  לשלוש: ירקות 🍅, שורשים ובצלים 🥔, עלים ותבלינים 🌿.
- מסמנים ✓ מוצר כדי להוסיף לרשימה, ואפשר להוסיף לו הערה (כמות, סוג...).
- **בקשות מיוחדות ⭐** — קטגוריה חופשית לבקשות חד-פעמיות.
- הוספת מוצרים חדשים לקטלוג בכל קטגוריה; "עריכת קטלוג" מאפשרת מחיקה.
- "שלח לקונה" — שולח את הרשימה המסודרת בוואטסאפ.
- "איפוס רשימה" — מנקה סימונים ובקשות מיוחדות לקראת הסבב הבא (הקטלוג נשמר).

### מסך "קנייה" 🛍️
- **🗺️ מסלול הקנייה** — מפה בצורת S: תחנה צבעונית לכל קטגוריה עם ספירה
  (למשל 2/5), סמן 🛒 על מיקום הקונה במסלול ודגל סיום 🏁. לחיצה על תחנה
  פותחת את רשימת המוצרים שלה, וגם משם אפשר לסמן שנאסף.
- רק הפריטים שסומנו, מקובצים לפי קטגוריות מתקפלות, עם מד התקדמות.
- הקשה על פריט מסמנת "נאסף" (הפריטים שנאספו יורדים לתחתית הקטגוריה).
- **מעקב מחיר ₪** — סימון פריט כ"נאסף" פותח אוטומטית את חלון המחיר:
  צילום תווית המחיר שעל המדף או השקית (זיהוי ספרות אוטומטי, עם אפשרות
  תיקון) או הזנה ידנית. הסכום המצטבר של העגלה מוצג למעלה ונכלל
  בהודעת "סיימתי".
- **⏱ מדידת זמן** — "התחל קנייה" מפעיל שעון עצר; "🏁 סיים" שומר את
  הקנייה בהיסטוריה.
- **📍 בחירת מקום קנייה** — "התחל קנייה" פותח בחירת חנות ממאגר מרכזי
  (עם חיפוש). מקום שלא במאגר (מכולת שכונתית וכו') מוסיפים בטופס קצר
  והוא נשמר במאגר לכל המשתמשים. הקנייה נרשמת על החנות והרשת.
- **🏷️ מבצעים** — כפתור שמציג מבצעים מהחנות (כרגע הדגמה; מתוכנן חיבור
  לחנויות שיזהו את הקונה וישדרו מבצעים בזמן אמת).
- כפתור וואטסאפ ליד כל פריט — שולח שאלה למי שרשם את הרשימה, עם תבניות
  מוכנות (למשל: "אין עגבניות שרי — להביא משהו דומה במקום?") או ניסוח חופשי.
- בסיום — עדכון "סיימתי" בוואטסאפ ואיפוס לרשימה הבאה.

### מסך "היסטוריה" 🧾
- כל קנייה שהסתיימה נשמרת: תאריך ושעה, המוצרים, המחירים, הסכום ומשך
  הקנייה.
- **📥 ייבוא לרשימה חדשה** — מתחילים קנייה חדשה מרשימה קודמת בלחיצה.

### מנוי משפחתי 👨‍👩‍👧 (דרך ⚙️ הגדרות)
- **יצירת משפחה** — שם משפחה + אזור מגורים + שם וטלפון; מתקבל קוד
  הצטרפות בן 6 תווים לשיתוף (יש כפתור שיתוף בוואטסאפ).
- **הצטרפות עם קוד** — כל בן משפחה נכנס עם הקוד + שם + טלפון נייד.
  בהגדרות רואים את כל מי שנרשם (שם, טלפון, תאריך הצטרפות) עם כפתור
  וואטסאפ ליד כל אחד.
- **לוח מודעות 📌** — במסך "מה צריך": כל בן משפחה כותב הודעה (למשל
  "צריך נייר טואלט"), כולם מקבלים התראה, ובלחיצת "🛒 לרשימה" ההודעה
  הופכת לפריט — אם המוצר קיים בקטלוג הוא מסומן בקטגוריה שלו, ואחרת
  נוסף לבקשות מיוחדות עם שם המבקש.
- **סנכרון אוטומטי** — הרשימה משותפת ומתעדכנת בין כל הטלפונים.
- **מעקב חי 🔴** — כשמישהו לוחץ "התחל קנייה", שאר בני המשפחה מקבלים
  איתות ורואים בזמן אמת מה כבר נאסף, את המחירים ואת שעון הקנייה.
- **בקשות תוך כדי קנייה 🔔** — בן משפחה בבית מסמן/מוסיף מוצר בזמן
  שהקנייה פעילה, והקונה מקבל התראה באפליקציה (עם רטט): "גל הוסיף/ה
  בקשה: עגבניות שרי (ירקות)" + באנר בקשות חדשות ותג 🆕 על הפריט עד
  שנאסף.
- **שאלות ממוקדות** — הקונה בוחר לאיזה בן משפחה לשלוח שאלה בוואטסאפ.
- **השוואה אזורית 📊** — במסך ההיסטוריה: סל ממוצע, מספר פריטים וזמן
  קנייה של המשפחה מול ממוצע אנונימי של משפחות אחרות באותו אזור.
- **השוואת רשתות 🏪** — טבלת השוואה בין רשתות (סל ממוצע, זמן, מספר
  קניות) על בסיס כל הקניות במערכת; רשתות שהמשפחה קונה בהן מסומנות ⭐.

### הגדרות ⚙️
- שם ומספר וואטסאפ של רושם/ת הרשימה — כפתורי השאלה יפתחו ישר את הצ'אט הנכון.
- אתחול מלא — החזרת קטלוג ברירת המחדל.

## הערות טכניות
- צד לקוח: `index.html` (HTML + CSS + JS). זיהוי המחיר מהתמונה
  משתמש ב-tesseract.js שנטען מ-CDN רק בשימוש הראשון; ללא אינטרנט —
  הזנה ידנית עדיין עובדת.
- צד שרת: `netlify/functions/api.mjs` — Netlify Function יחידה בנתיב `/api`.
  ללא משפחה האפליקציה עובדת גם לגמרי מקומית.
- **מסד נתונים מרכזי**: Netlify DB (Postgres של Neon). סכמה רלציונית:
  `families` (משפחות + קוד הצטרפות), `members` (שם + נייד לכל בן משפחה),
  `sessions` (נתוני התחברות: יצירה/הצטרפות, מכשיר, נראה לאחרונה),
  `lists` (הרשימה המסונכרנת, JSONB), `purchases` (לוג קניות מלא —
  תאריך, משך, סכום, פריטים — עם אינדקסים לפי משפחה ואזור לבנצ'מארק),
  `stores` (מאגר מקומות קנייה: רשת + סניף + אזור, נבנה על-ידי
  המשתמשים), `deals` (מבצעים לפי חנות/אזור — מוכן לחיבור רשתות בעתיד).
  הטבלאות נוצרות אוטומטית בבקשה הראשונה.
- **הפעלת ה-DB (חד-פעמי)**: בממשק Netlify — Extensions ← Neon (או
  `npx netlify db init` בטרמינל מקומי). עד אז הפונקציה נופלת אוטומטית
  ל-Netlify Blobs, וברגע שה-DB פעיל — משפחות קיימות עוברות אליו
  אוטומטית (מיגרציה עצלה בזמן הבקשה). בדיקת מצב: `/api?op=status`
  (מחזיר `db: true/false`).
- סנכרון: דחיפה אוטומטית אחרי כל שינוי (debounce ~1.2ש) ומשיכה כל
  8 שניות + בחזרה לאפליקציה. קונפליקטים נפתרים לפי "הכתיבה האחרונה
  גוברת".
- נתוני ההשוואה האזורית אנונימיים לחלוטין — נשמרים רק מספרים (סכום,
  מספר פריטים, משך) לפי אזור, בלי שום זיהוי משפחה.
- מפתח האחסון: `shopping-app-v1` ב-localStorage (סכמה v2; נתוני v1
  עוברים מיגרציה אוטומטית); פרטי המשפחה ב-`shopping-family-v1`.
- שליחת וואטסאפ דרך קישורי `wa.me`; מספרים ישראליים (05X...) מנורמלים
  אוטומטית לפורמט בינלאומי (972...).
